import { ConversorDiaPipe } from './conversor-dia.pipe';

describe('ConversorDiaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversorDiaPipe();
    expect(pipe).toBeTruthy();
  });
});

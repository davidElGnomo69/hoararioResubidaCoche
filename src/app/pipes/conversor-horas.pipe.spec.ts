import { ConversorHorasPipe } from './conversor-horas.pipe';

describe('ConversorHorasPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversorHorasPipe();
    expect(pipe).toBeTruthy();
  });
});
